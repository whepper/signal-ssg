[package]
name = "signal-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Canonical immutable site model, identities, queries, and core diagnostics for Signal SSG"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
toml = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
